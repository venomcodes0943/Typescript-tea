const intro = (name: string, lang: string, email: string): string => {
    // Do something Here
    return `Hello Im ${name} Learing ${lang} Mail Me On ${email}`;
};

let firstMeet = intro("Haseeb", "typeScript", "dhcodes0943@gmail.com");

console.log(firstMeet);

export { }